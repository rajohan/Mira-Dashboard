pre-commit:
    parallel: true
    commands:
        format:
            run: bun run check format
        lint:
            run: bun run check lint

pre-push:
    commands:
        check:
            run: bun run check
