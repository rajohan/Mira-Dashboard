/** Maximum concurrent run slots declared by one durable worker. */
export const jobWorkerCapacityMaximum = 16;
/** Maximum durable worker rows retained in one browser-facing summary. */
export const jobWorkerSummaryMaximum = 32;
