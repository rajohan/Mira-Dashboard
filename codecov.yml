coverage:
    precision: 2
    round: down
    range: 20..85
    status:
        project:
            backend:
                target: 85%
                threshold: 0%
                informational: false
                if_ci_failed: error
                flags:
                    - backend
                paths:
                    - backend/src/
            frontend:
                target: 85%
                threshold: 0%
                informational: false
                if_ci_failed: error
                flags:
                    - frontend
                paths:
                    - frontend/src/
        patch:
            backend:
                target: 85%
                threshold: 0%
                informational: false
                if_ci_failed: error
                flags:
                    - backend
                paths:
                    - backend/src/
            frontend:
                target: 85%
                threshold: 0%
                informational: false
                if_ci_failed: error
                flags:
                    - frontend
                paths:
                    - frontend/src/

comment:
    layout: "reach,diff,flags,tree"
    behavior: default
    require_changes: false

flags:
    frontend:
        paths:
            - frontend/src/
    backend:
        paths:
            - backend/src/
